name: WB Worker Test 6

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "base step"

      - name: Conditional step
        if: ${{ secrets.WB_ANTHROPIC_API_KEY != '' }}
        run: echo "would run"
