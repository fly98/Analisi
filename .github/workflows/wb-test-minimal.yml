name: WB Worker Test 7

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - run: echo "base step"

      - name: Conditional step existing secret
        if: ${{ secrets.WB_ADMIN_KEY != '' }}
        run: echo "would run"
